services:
  - type: web
    name: telegram-kimi-bot
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python telegram_bot.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
